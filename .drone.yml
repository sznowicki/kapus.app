---
kind: pipeline
type: exec
name: on-host
depends_on:
  - ci
steps:
  - name: build
    when:
      branch:
        - main
    environment:
      CI_DEPLOY_PWD:
        from_secret: CI_DEPLOY_PWD
    commands:
      - cd CI_DEPLOY_PWD
      - ./deploy.sh
